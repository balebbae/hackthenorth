# isDisjoint

Source: https://www.nianticspatial.com/docs/api/swift/NSDK.SceneSegmentationChannels.method-isDisjoint/

isDisjoint

---

## Declaration

func isDisjoint(with other: Self) -> Bool

#### Summary

Returns a Boolean value that indicates whether the set has no members in
common with the given set.
In the following example, the employees set is disjoint with the
visitors set because no name appears in both sets.
    let employees: Set = ["Alicia", "Bethany", "Chris", "Diana", "Eric"]
    let visitors: Set = ["Marcia", "Nathaniel", "Olivia"]
    print(employees.isDisjoint(with: visitors))
    // Prints "true"

#### Returns

true if the set has no elements in common with other;
  otherwise, false.

### Parameters

| Name | Type | Summary |
| --- | --- | --- |
| other | Self | - |

---
